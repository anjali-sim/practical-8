import styled from "styled-components";

// Style for the dropdown present in the user list
const DropDownStyle = styled.select`
  border: 1px solid rgba(0, 0, 0, 0.1);
  border-radius: 3px;
  padding: 6px;
  padding-right: 6px;
  outline: none;
`;

export default DropDownStyle;
